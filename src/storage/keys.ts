const STORAGE_PREFIX = '@elden-ring-companion';

export const storageKeys = Object.freeze({
  settings: `${STORAGE_PREFIX}/settings:v1`,
  defeatedBossIds: `${STORAGE_PREFIX}/defeated-boss-ids:v1`,
});
